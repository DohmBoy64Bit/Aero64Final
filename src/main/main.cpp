#include <cstdio>

int main(int argc, char** argv) {
  (void)argc;
  (void)argv;
  std::puts("[Aero64Recompiled] Bootstrap build. Run from repo root; place USA .z64 under roms/.");
  std::puts("[Aero64Recompiled] Next: add lib/ engine submodule + RecompiledFuncs target per Docs/Workflow.md.");
  return 0;
}
