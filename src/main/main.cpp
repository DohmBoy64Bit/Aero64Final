#include <cstdio>

int main(int argc, char** argv) {
  (void)argc;
  (void)argv;
  std::puts("[Aero64Recompiled] Bootstrap build. Run from repo root; ROM: roms/afa.n64.us.z64 (see Docs/Workflow.md).");
  std::puts("[Aero64Recompiled] Next: parallel tracks — N64Recomp + lib/CMake per Docs/Workflow.md Phase 3 \"Parallel priorities\".");
  return 0;
}
